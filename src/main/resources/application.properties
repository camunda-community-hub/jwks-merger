server.port=8080

jwks.upstream-urls[0]=${JWKS_UPSTREAM_URLS_0:}
jwks.upstream-urls[1]=${JWKS_UPSTREAM_URLS_1:}
jwks.refresh-interval-ms=${JWKS_REFRESH_INTERVAL_MS:60000}

management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never
logging.level.com.example.jwksmerger=INFO
